import{_ as a,c as t,a2 as o,o as i}from"./chunks/framework.bs4cssDz.js";const p=JSON.parse('{"title":"Backgrounds","description":"","frontmatter":{},"headers":[],"relativePath":"documentation/theming/background.md","filePath":"documentation/theming/background.md"}'),n={name:"documentation/theming/background.md"};function r(s,e,l,d,c,h){return i(),t("div",null,e[0]||(e[0]=[o(`<h1 id="backgrounds" tabindex="-1">Backgrounds <a class="header-anchor" href="#backgrounds" aria-label="Permalink to &quot;Backgrounds&quot;">​</a></h1><p>SukiUI includes a background renderer which utilises GPU acceleration and shaders to draw complex runtime background effects that update with theme changes.</p><p>All properties mentioned on this page can be accessed out of the box via the <code>Background{Property}</code> properties present in <code>SukiWindow</code>. If you wish to host a <code>SukiBackground</code> control of your own in another context then these properties should be similarly named without the <code>Background</code> prefix.</p><h2 id="styles" tabindex="-1">Styles <a class="header-anchor" href="#styles" aria-label="Permalink to &quot;Styles&quot;">​</a></h2><p>Changing style to one of the defaults is done via <code>BackgroundStyle</code> - Available out of the box are <code>Gradient</code>, <code>Flat</code> and <code>Bubble</code>. It is possible however to use custom shaders (written in SKSL) to create your own backgrounds.</p><p>There are two methods to use your own shaders written in this language:</p><h4 id="backgroundshaderfile" tabindex="-1">BackgroundShaderFile <a class="header-anchor" href="#backgroundshaderfile" aria-label="Permalink to &quot;BackgroundShaderFile&quot;">​</a></h4><p>By including an embedded resource in your application with the correct file extension (E.G. <code>MyShader.sksl</code>) it is possibly to simply set the <code>BackgroundShaderFile</code> property to the name of the file (without the extension). SukiUI will search your embedded resources for the file and load the shader for use automatically.</p><p>SukiUI includes a few shaders that are not included in the default enum but are nonetheless accessible this way including <code>Cells</code> and <code>Waves</code>.</p><h4 id="backgroundshadercode" tabindex="-1">BackgroundShaderCode <a class="header-anchor" href="#backgroundshadercode" aria-label="Permalink to &quot;BackgroundShaderCode&quot;">​</a></h4><p>Simply assigning a string representing your shader to this property will create the runtime effect and render it without the need for embedded files, it is possible to bind to this property and dynamically alter the shader code at runtime if you so desire, any change will be reflected immediately.</p><h4 id="notes" tabindex="-1">Notes <a class="header-anchor" href="#notes" aria-label="Permalink to &quot;Notes&quot;">​</a></h4><p>When choosing which background style to render, if more than one property is assigned to a non-null value at once, then SukiUI will prioritize the properties in the following order <code>BackgroundShaderFile</code> -&gt; <code>BackgroundShaderCode</code> -&gt; <code>BackgroundStyle</code></p><h2 id="custom-styles" tabindex="-1">Custom Styles <a class="header-anchor" href="#custom-styles" aria-label="Permalink to &quot;Custom Styles&quot;">​</a></h2><p>In order to render custom backgrounds via the properties mentioned before, it is necessary to write <a href="https://github.com/google/skia/blob/main/src/sksl/README.md" target="_blank" rel="noreferrer">SKSL</a>.</p><p>This might seem daunting at first but it is a fairly simple language in which you write an entry point function that returns a <code>vec4</code> representing the colour of each pixel. With GPU parallelization it is possible to execute large numbers of fairly complex mathematical calculations very quickly.</p><p>SukiUI automatically includes a set of uniforms in these effects before shader compilation for you to use in your shaders, these are:</p><ul><li><code>float iTime</code> - A representation of the current ticks since the background began rendering. Only changed when animation is enabled.</li><li><code>float iDark</code> - Whether the application is in light or dark mode - 0 = light, 1 = dark.</li><li><code>float iAlpha</code> - The alpha at which the background is being drawn, largely controlled by opacity of the background control.</li><li><code>vec3 iResolution</code> - The resolution of the background in pixels, <code>x</code> and <code>y</code> are the only elements used, <code>z</code> is always 0.</li><li><code>vec3 iPrimary</code> - A (not exact) representation of the current active primary theme color.</li><li><code>vec3 iAccent</code> - A (not exact) representation of the current active accent theme color.</li><li><code>vec3 iBase</code> - A pre-calculated background colour for the theme - or an off-white if the app is in light mode.</li></ul><p>These uniforms are included automatically in any shader file or code you supply and can be used as if they were globally scoped variables. They are updated per-frame and any change in the application state will be reflected in these uniforms.</p><p>The simplest example of a shader is our <code>Flat</code> style which simply returns the base background colour for every pixel.</p><div class="language-glsl vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">glsl</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">vec4</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> main</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(vec2 </span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">fragCoord</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) {</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    return</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> vec4</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(iBase, iAlpha);</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><h2 id="transitions" tabindex="-1">Transitions <a class="header-anchor" href="#transitions" aria-label="Permalink to &quot;Transitions&quot;">​</a></h2><p>Enabling <code>BackgroundTransitionsEnabled</code> will cause any change in the background style to &quot;fade&quot; between the two with a simple opacity switch, this is where the <code>iAlpha</code> property is mostly used.</p><p><code>BackgroundTransitionTime</code> can also be set to define the time in seconds for the transition (default: 1 second).</p><h2 id="animation" tabindex="-1">Animation <a class="header-anchor" href="#animation" aria-label="Permalink to &quot;Animation&quot;">​</a></h2><p>The background renderer does support animation via <code>BackgroundAnimationEnabled</code> and will run at native framerates, all default SukiUI backgrounds support animation out of the box (except for <code>Flat</code>).</p><div class="warning custom-block"><p class="custom-block-title">WARNING</p><p>Due to the need for an invalidation and repaint of the entire <a href="https://docs.avaloniaui.net/docs/concepts/control-trees#visual-tree" target="_blank" rel="noreferrer">visual tree</a>, animations can have a significant impact on performance and it is recommended that you only use it if absolutely necessary. In limited testing we see a roughly 5% bump in CPU and 20% bump in GPU utilization <em>at all times</em> when animation is enabled.</p></div><h2 id="software-fallback" tabindex="-1">Software fallback <a class="header-anchor" href="#software-fallback" aria-label="Permalink to &quot;Software fallback&quot;">​</a></h2><p>In the case <a href="https://github.com/mono/SkiaSharp" target="_blank" rel="noreferrer">SkiaSharp</a> (Avalonia&#39;s underlying rendering engine) is unable to find any usable hardware acceleration, SukiUI will fall back to a software rendering solution which is simply the <code>Flat</code> background style rendered on the CPU.</p>`,29)]))}const g=a(n,[["render",r]]);export{p as __pageData,g as default};
